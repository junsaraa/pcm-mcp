# Pin the MCP SDK to the stable 1.x line: `pip install mcp` now pulls 2.x,
# whose API differs. Move to <2 only after testing against v2.
mcp>=1.28,<2
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27
pyyaml>=6.0
jsonschema
